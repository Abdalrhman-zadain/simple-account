# Phase 1: Accounting Foundation

Implemented domain area for the accounting engine.

- Owns chart of accounts, journal entries, posting, ledger, audit, fiscal, and master data
- Exposes the current accounting API surface without changing route paths
- Depends on `platform/auth` and `common/prisma`
